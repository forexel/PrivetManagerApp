# Privet Manager Portal (frontend)

Single Page Application for managers to access client onboarding flows. Bootstrapped with Vite + React.

## Scripts

```bash
npm install
npm run dev      # http://localhost:5174
npm run build
npm run preview
```

Configure backend proxy by setting `VITE_API_BASE` (defaults to `/api/manager`).
